# 微信公众号API发布经验总结

**最后更新**: 2026-03-06

## 问题背景

在发布公众号文章时，遇到脚本无法自动读取凭据的问题。

## 解决方案

### 1. 凭据配置位置

- **正确位置**: `/root/.openclaw/workspace/skills/content-factory/.env`
- **凭据来源**: `/root/.openclaw/credentials/wechat.json`

### 2. 配置步骤

```bash
# 读取凭据
cat /root/.openclaw/credentials/wechat.json | jq -r 'to_entries | map("\(.key)=\(.value)") | join("\n")'

# 写入 .env 文件
# WECHAT_APP_ID=wxf9400829e3405317
# WECHAT_APP_SECRET=f122506bb959353211175841f8ed039d
```

### 3. 正确使用方式

**错误方式**:
```bash
# ❌ 在命令行临时设置环境变量
WECHAT_APP_ID=xxx python3 script.py
```

**正确方式**:
```bash
# ✅ 直接运行脚本（会自动读取 .env）
cd /root/.openclaw/workspace/skills/content-factory
python3 -X utf8 scripts/wechat_publish.py --html "path/to/article.html"
```

### 4. 发布命令

```bash
cd /root/.openclaw/workspace/skills/content-factory
python3 -X utf8 scripts/wechat_publish.py --html "/root/.openclaw/workspace/output/2026-03-06-xxx.html"
```

**注意**: 必须加 `-X utf8` 参数，否则中文字符会变成 `\uXXXX` 编码

### 5. API 权限限制

- ✅ 获取access token: 成功
- ✅ 上传封面图片: 成功
- ✅ 创建草稿(draft): 成功
- ❌ 提交预览(preview): API未授权（需要更高权限）

**结果**: 草稿会自动创建，需登录 https://mp.weixin.qq.com/ 手动发布

## 关键要点

1. **凭据写入 .env**: Content Factory Skill的脚本会自动读取 `skills/content-factory/.env` 文件
2. **使用 -X utf8**: Windows环境下必须加这个参数防止编码问题
3. **手动发布**: 由于API权限限制，草稿创建后需手动在后台发布

## 重大错误：价格数据编造问题

**时间**: 2026-03-06

**错误描述**:
在撰写GPT-5.4 vs Claude对比文章时，我编造了GPT-5.4的API价格数据。

**错误数据**: $30/M (输入) / $180/M (输出)
**正确数据**: $2.5/M (输入) / $15/M (输出) - 来自platform.openai.com

**错误原因**:
1. 没有第一时间从官方网站核实数据
2. 主观猜测价格数据
3. 写完文章后才去查证，而不是之前

**深刻反思**:
- ❌ 禁止在文章中编造任何数据
- ❌ 价格、参数、排名等信息必须来自官方或权威来源
- ✅ 必须先查证，再写作
- ✅ 如果不确定，明确标注"待核实"

**正确流程应该是**:
1. 确定主题
2. **先查证所有数据**（官网、权威报道）
3. 整理信息
4. 写作
5. 再次核实

## 待核实问题

1. **OpenClaw是否支持GPT-5.4**: 从GitHub看到支持OpenAI，但未明确列出GPT-5.4，需进一步确认
2. **GPT-5.4订阅方式**: $20/月起的价格来源待核实

## 相关文件

- 凭据: `/root/.openclaw/credentials/wechat.json`
- .env: `/root/.openclaw/workspace/skills/content-factory/.env`
- 发布脚本: `/root/.openclaw/workspace/skills/content-factory/scripts/wechat_publish.py`
- HTML模板规范: Content Factory SKILL.md

---

## 2026-03-06 14:30 飞书语音 Skill 固化

### 已完成
- 将飞书语音播放能力固化为 Skill：`skills/feishu-voice-reply/`
- 已创建脚本：`scripts/build_feishu_voice.py`
- 技术链路验证通过：Edge TTS → ffmpeg → Ogg/Opus → Feishu 语音播放条
- 已打包：`dist/skills/feishu-voice-reply.skill`
- 统一本地生效路径：`/root/.openclaw/workspace/skills/feishu-voice-reply`

### 模型配置更新
- Main / Coding：GPT-5.4 → Minimax M2.5 → Kimi 2.5
- 其他 Sub Agent 默认：Minimax M2.5 → Kimi 2.5

### 备注
- Skill 内容已检查，无私人敏感信息、无凭据、无 Token
- 若需发布到 ClawHub，当前阻塞点是本机 `clawhub` 尚未登录，需要先完成 `clawhub login`

---

## 14:20 新增记录

### 飞书语音播放能力固化为 Skill
- 需求：老板要求“语音播放”时，不是发 mp3 文件路径，而是在飞书里直接展示可播放/暂停的原生播放条
- 验证通过链路：**Edge TTS → ffmpeg 转 Ogg/Opus → Feishu asVoice 发送**
- 已创建 Skill：`skills/feishu-voice-reply/`
- 核心脚本：`skills/feishu-voice-reply/scripts/build_feishu_voice.py`
- 打包产物：`dist/skills/feishu-voice-reply.skill`
- 结论：后续在飞书里做语音播放，默认同时发送**文本 + Ogg/Opus语音消息**

### Agent 模型分工更新
- Main / Coding：`openai-code/gpt-5.4` → `minimax-cn/MiniMax-M2.5` → `kimi-coding/k2p5`
- 其他 Sub Agent 默认：`minimax-cn/MiniMax-M2.5` → `kimi-coding/k2p5`
- 纠偏：旧记忆里“Chief/Content/Coding 用 MiniMax、Growth/Product/Finance 用 Kimi”的结论已过期，不再适用

